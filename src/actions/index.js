export const SELECT_CARD = cardId => ({
  type: 'SELECT_CARD',
  cardId,
});

export const ON_ENTER_CARD = cardId => ({
  type: 'ON_ENTER_CARD',
  cardId,
});

export const ON_LEAVE_CARD = cardId => ({
  type: 'ON_LEAVE_CARD',
  cardId,
});
